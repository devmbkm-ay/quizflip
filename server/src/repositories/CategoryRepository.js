// temporary stub until category model exists
const categoryRepository = {
  async findOne(filter) {
    // return null to indicate not found; adapt later
    return null;
  },
};

export default categoryRepository;
